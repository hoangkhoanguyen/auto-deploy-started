on:
  push:
    branches:
      - 'main'
name: build, publish images
jobs:
  build:
    name: build and publish images
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - name: npm install
        uses: actions/npm@main
        with:
          args: install
      - name: login to docker hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}
      - name: set up docker buildx
        uses: docker/setup-buildx-action@v2
      - name: build and push
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/nodejstest:latest
          file: ./Dockerfile
      # - name: docker build
      #   uses: actions/docker/cli@main
      #   with:
      #     args: build -t ${{ secrets.DOCKER_USERNAME }}/nodejstest:latest .
      # - name: docker login
      #   uses: actions/docker/login@main
      #   env:
      #     DOCKER_PASSWORD: ${{ secrets.DOCKER_PASSWORD }}
      #     DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      # - name: docker push
      #   uses: actions/docker/cli@main
      #   with:
      #     args: push ${{ secrets.DOCKER_USERNAME }}/nodejstest:latest
